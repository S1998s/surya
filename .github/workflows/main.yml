name: Test Autograde

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: test-autograde
        uses: masaditya/test-autograde@v10.0
        with:
          kelas: ""
          minggu-ke: ""
